NODE_ENV=development    # local laptop, not production
PORT=3000               # host and container both use 3000 in this guide
APP_VERSION=local       # label so GET / shows you are not on a deployed build